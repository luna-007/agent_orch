[{
    "type": "function",
    "function": {
        "name": "get_disk_usage",
        "description": "Used to calculate the total, used and free space of the disk",
        "parameters": {
            "type": "object",
            "properties": {
                "type": {                    
                    "type": "string",
                    "description": "The specific Storage type to retreive",
                    "enum": ["Total", "Used", "Free"]
                }
            },
            "required": ["type"]
        }
    }
},
{
    "type": "function",
    "function": {
        "name": "get_time",
        "description": "Used to get the current time, date, timezone and combined date and time",
        "parameters": {
            "type": "object",
            "properties": {
                "type" : {
                    "type": "string",
                    "description": "The specific time or date or timezone or combined data to retreive",
                    "enum": ["date", "time","timezone","both"]
                },
                "time_zone": {
                    "type": "string",
                    "description": "This is for updating the timezone data like Asia/Kolkata, America/New_York or Europe/London"

                }
            },
            "required": ["type"]
        }
    }
},
{
    "type": "function",
    "function": {
        "name": "fetch_web_content",
        "description": "Used to access the Internet and get raw text details from a webpage.",
        "parameters": {
            "type": "object",
            "properties": {
                "url" : {
                    "type": "string",
                    "description": "The absolute HTTP or HTTPS URL of the webpage to fetch (e.g., https://en.wikipedia.org)"
                }
            },
            "required": ["url"]
        }
    }
},
{
    "type": "function",
    "function": {
        "name": "search_local_files",
        "description": "Used to access the local file system of the device and move through locations",
        "parameters": {
            "type": "object",
            "properties": {
                "directory" : {
                    "type": "string",
                    "description": "The absolute directory path which contains files or there could be directories as well, which can further be storing files"
                },
                "keyword": {
                    "type": "string",
                    "description": "This is the keyword we are searching for and have to find the match"
                }
            },
            "required": ["directory", "keyword"]
        }
    }
},
{
    "type": "function",
    "function": {
        "name": "list_directory_contents",
        "description": "Used to get the files and directories inside a directory",
        "parameters": {
            "type": "object",
            "properties": {
                "directory" : {
                    "type": "string",
                    "description": "The absolute directory path which contains files and directories as well, which can further be storing files"
                }
            }
        }
    }
},
{
    "type": "function",
    "function": {
        "name": "change_directory",
        "description": "Changes the active current working directory of this session (cd).",
        "parameters": {
            "type": "object",
            "properties": {
                "path" : {
                    "type": "string",
                    "description": "The folder name, relative path (e.g., '..', 'services'), or absolute path to move to"
                }
            },
            "required": ["path"]
        }
    }
},
{
    "type": "function",
    "function": {
        "name": "get_current_directory",
        "description": "Prints the active, current working directory path of the session(pwd)",
        "parameters": {
            "type": "object",
            "properties": {}
        }
    }
}]